<div class="container container__user-meals">
  <h1>Mes repas</h1>
  <% if @meals.empty? %>
    <p>Tu n'as pas encore saisi de repas. Commence tout de suite en cliquant 👉 <%= link_to "ici", new_meal_path %></p>
  <% else %>
    <ul>
      <% @meals.each do |m| %>
        <li class="user-meal">
          <%= link_to m.name.capitalize, meal_path(m) %>
          <%= link_to meal_path(m), data: { turbo_method: :delete, turbo_confirm: "Supprimer ce repas ?"} do %>
            <i class="fa-solid fa-trash-can"></i>
          <% end %>
        </li>
      <% end %>
    </ul>
  <% end %>
</div>
