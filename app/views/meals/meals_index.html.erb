<div class="container container__user-meals">
<h1>Mes repas</h1>
  <ul>
    <% @meals.each do |m| %>
      <li class="user-meal">
        <%= link_to m.name, meal_path(m) %>
        <%= link_to meal_path(m), data: { turbo_method: :delete, turbo_confirm: "Supprimer ce repas ?"} do %>
          <i class="fa-solid fa-trash-can"></i>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>
