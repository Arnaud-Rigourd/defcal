<div class="container__form">
  <h4>Choisi le produit correspondant dans la liste, ou affine ta recherche 👇</h4>

  <%= simple_form_for [meal, food] do |f| %>
    <%= f.input :name,
                value: food.name,
                placeholder: "ingrédient" %>
    <%= f.submit "Refresh 🔍"%>
  <% end %>

</div>
