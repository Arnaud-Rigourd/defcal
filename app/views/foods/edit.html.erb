<div class="container">
  <div class="container__page" data-controller="products">
    <%#= render "shared/btn_home" %>

    <h3>Ton produit est introuvable ?</h3>
    <%= render partial:"shared/form_new_callback", locals: {meal: @meal, food: @food} %>

    <h2>Selectionne ton produit dans la liste (pense à scroller) 👇</h2>
    <div class="results">
      <ul>
        <% @products.each do |p| %>
          <li data-action="click->products#addInfo" data-products-target="product" data-product-name="<%= p.product_name %>" data-product-code="<%= p.code %>"><%= image_tag p.image_small_url, alt:"food image" %><%= p.product_name %></li>
        <% end %>
      </ul>
    </div>

    <%= render partial:"shared/form_edit", locals: {meal: @meal, food: @food} %>

    <%# <%= render partial:"shared/btn_meal", locals: {meal: @meal} if @meal.foods.length > 1 %>
  </div>
</div>
